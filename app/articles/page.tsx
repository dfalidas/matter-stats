import { PageShell } from "../_components/page-shell";

export default function ArticlesPage() {
  return (
    <PageShell
      eyebrow="Articles"
      title="Article library"
      description="Placeholder for searchable article history, read status, highlights, and reading metadata."
    />
  );
}

import { Card } from "@/components/ui/card";

export default function ReportsPage() {
  return <Card><h1 className="text-3xl font-semibold">Reports</h1><p className="mt-3 text-muted-foreground">Daily, weekly, monthly, yearly, and all-time report views will build on the shared analytics data layer.</p></Card>;
}
